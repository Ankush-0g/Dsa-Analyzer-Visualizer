services:
  - type: web
    name: algoviz-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    autoDeploy: true
    envVars:
      - key: PORT
        value: "8080"
      - key: JAVA_OPTS
        value: "-Xms256m -Xmx512m"
    healthCheckPath: "/"
